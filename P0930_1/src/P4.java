public class P4 {
    public static void main(String[] args) {
        // 연산자 Operator - 연산 결과의 타입에 매우 주의를 기울일 것(특히 연산 결과)
        // 산술 연산자: + - * / %
        // 비교 연산자: == != > < >= <=
        // 논리 연산자: && || !
        // 단추배정 연산자: += -= *= ...
        // 증감 연산자: ++ --

        // 연산자 우선 순위는 자연스럽게 적용됨
    }
}
