<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webboard.repository.mapper.CommentMapper">

    <!-- 댓글 작성 -->
    <insert id="insertComment" parameterType="com.webboard.entity.Comment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comments (post_id, user_id, content)
        VALUES (#{postId}, #{userId}, #{content})
    </insert>

    <!-- 댓글 조회 (ID로) -->
    <select id="selectCommentById" parameterType="int" resultType="com.webboard.entity.Comment">
        SELECT c.comment_id as commentId, c.post_id as postId, c.user_id as userId, 
               c.content, c.created_at as createdAt, u.username
        FROM comments c
        LEFT JOIN users u ON c.user_id = u.user_id
        WHERE c.comment_id = #{commentId}
    </select>

    <!-- 게시글별 댓글 조회 -->
    <select id="selectCommentsByPostId" parameterType="int" resultType="com.webboard.entity.Comment">
        SELECT c.comment_id as commentId, c.post_id as postId, c.user_id as userId, 
               c.content, c.created_at as createdAt, u.username
        FROM comments c
        LEFT JOIN users u ON c.user_id = u.user_id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>

    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="com.webboard.entity.Comment">
        UPDATE comments
        SET content = #{content}
        WHERE comment_id = #{commentId}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="deleteComment" parameterType="int">
        DELETE FROM comments
        WHERE comment_id = #{commentId}
    </delete>

    <!-- 게시글의 모든 댓글 삭제 -->
    <delete id="deleteCommentsByPostId" parameterType="int">
        DELETE FROM comments
        WHERE post_id = #{postId}
    </delete>

</mapper>
